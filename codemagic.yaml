echo "== Verifying contents of the .app bundle =="
APP_BUNDLE="$ARCHIVE_PATH/Products/Applications/$APP_NAME.app"

if [ ! -f "$APP_BUNDLE/$APP_NAME" ]; then
  echo "❌ Executable $APP_NAME not found in .app bundle"
  ls -l "$APP_BUNDLE"
  exit 1
fi
